//
//  Favorites.swift
//  FlyttaTest
//
//  Created by Moa Morfelt on 2021-12-03.
//

/*import SwiftUI

class Favorites: ObservableObject {
    private var category2: Set<String>
    
    private let saveKey = "Favorites"
    
    init() {
        //load our saved data
        
        self.category2 = []
    }
    
    func contains( category2: item) -> Bool {
        resorts.contains(resort.id)
    }
    func add( category2: item) {
        objectWillChange.send()
        resorts.inser(resort.id)
        save()
    }
    func add( category2: item) {
        objectWillChange.send()
        resorts.remove(resort.id)
        save()
}
    func save() {
        // write out our data
    }
 }*/
